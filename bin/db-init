#!/usr/bin/env python

from sqlalchemy import create_engine
from jae.db.models import BaseModel as model
from jae.common import cfg

CONF=cfg.CONF

 
engine = create_engine('mysql://nae:nae@localhost/nae')
model.metadata.create_all(engine)
